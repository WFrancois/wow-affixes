{
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "dev-server": "encore dev-server",
        "dev": "encore dev",
        "watch": "encore dev --watch",
        "build": "encore production",
        "docker:build": "docker build -t wow-affixes .",
        "docker:run": "docker run --rm -p 8080:80 --name wow-affixes wow-affixes"
    },
    "dependencies": {
        "@symfony/webpack-encore": "^0.30.2",
        "bootstrap": "^3.4.1",
        "jquery": "^3.5.1",
        "node-sass": "^4.14.1",
        "sass-loader": "^8.0.2",
        "webpack-notifier": "^1.13.0"
    }
}
